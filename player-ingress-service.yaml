apiVersion: v1
kind: Service
metadata:
  name: player-ingress-backend-service
  namespace: default
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: player-ingress-backend-service
  type: NodePort